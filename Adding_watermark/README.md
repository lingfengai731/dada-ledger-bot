# DADA Watermark 🔏

给 DADA 的婚礼 / 花艺作品加水印的小工具。

最近我们的图在各个社交平台被人盗用,有的还直接说成是自己的作品。这个工具就为两件事:让盗图更费劲,以及万一真被盗,我们能拿出证据说"这是我们的"。

办法是给每张图叠两层水印:

- **看得见的那层**:角落一枚淡淡的 DĀDA ISLAND 白色 Logo(透明度/大小/位置可调)。挡不住有心人,但能劝退顺手转发的。
- **看不见的那层**:藏在像素里的一段编码,肉眼完全看不出来。图被压缩、转发之后它还在;把盗图拖进验证页就能解出来,证明出处。

每张图的隐形编码会记到一张本地登记表,对应"哪场婚礼、哪天"。所以验证时不只是告诉你"是 DADA 的",还能说出"这是 6 月 16 号 Komaneka 那场 Coco 的图"。

> 🌐 **线上地址**:https://watermark-dada-island.onrender.com (部署在 Render;用 `WATERMARK_PASSWORD` 登录)。

## 快速开始

```bash
pip install -r requirements.txt
python -m uvicorn app:app --port 8000
```

打开 http://localhost:8000,两个页面:

- **加水印**:**拖一张或一批图**进去(支持拖拽 / 批量),填上是哪场婚礼(不填也行),可调**可见 logo 的位置 / 浓淡 / 大小**,提交。单张直接下载;一批则给"打包下载(zip)"。隐形编码都已登记。
- **验证盗图**:把可疑的图拖进来。是我们的,它会显示编码 + 哪场婚礼;不是,它会直接说没找到水印。

> 🌐 **三语界面**:右上角可一键切换 **中文 / English / 印尼语**;首次访问按浏览器语言自动选,之后记住你的选择。

### 那两个输入框(Shoot/Client、Note)填什么?要紧吗?

**它们不会画在图片上,也不会改变水印的样子。** 图上看得见的永远是 DADA logo,看不见的永远是一段随机编码。这两个框只是给这段编码贴个"标签",存进登记表——

- **Shoot / Client**:这张图是哪场婚礼 / 哪个客户,例如 `Coco — Komaneka 16 Jun`。
- **Note**:随便备注,可留空,例如 `IG 发布` / `已交付客户`。

作用是:**以后验证盗图时,工具能告诉你"这是 Coco 那场的图",而不只是一串编码。** 不填也能正常加水印、正常验证,只是验证时少了这个"是哪场"的说明。两个都可留空。

> 手机网页端可用(自适应)。提交后会跳到结果页,刷新/返回都正常。

想确认隐形水印真的扛得住压缩,又不想开浏览器,跑一下:

```bash
python test_roundtrip.py
```

它会加完水印,再模拟平台把图压成 q80、q60,看还能不能解回来。

## 它扛得住什么

隐形水印用的是频域算法(DWT-DCT),实测:

| 盗图常见的折腾 | 还认得出吗 |
|---|---|
| 重新压缩(转发、截图最常见) | 能,压到 q60 都行 |
| 稍微缩放 | 大多能 |
| 大幅裁剪、旋转、只截一小角 | 多半不行 |

说句实话:没有哪种隐形水印是抹不掉的。它真正解决的是最常见的那种盗图——整张图被截走、重新压缩、再发出去。要做到"裁了一角也认得出"、还能全网自动揪盗图,那得上 Digimarc、Imatag 这类收费服务,以后需要再说。

## 它是怎么搭的

- `watermark.py` —— 水印引擎。每张图先**统一成干净的 RGB**(自动转向、定上限尺寸),再用 Pillow 叠可见 Logo,最后用 `blind-watermark` 把隐形编码埋进频域。统一这一步能吃下各种格式(含 iPhone 的 **HEIC**、PNG 透明图、CMYK、16 位),不再因格式报错。
- `app.py` —— FastAPI 网页,接口:加水印、下载、验证、`/healthz`(保活),外加一张 SQLite 登记表。单张图坏了只跳过它并提示原因,不会整批 500。
- 验证用的是**模糊匹配**。压缩之后解出来的编码偶尔会差一两个字符,所以拿它跟登记表里每个编码比"差几个字",最接近、又足够近的就算命中。这样压过的图也认得出。

## 性能 / 速度(Render 免费版)

- **冷启动**:免费版闲置 15 分钟会休眠,首次访问要等 30-60 秒唤醒——这是"等很久"的主因。
  解法:用 **UptimeRobot / cron-job.org** 每 5 分钟访问一次 `/healthz`(HTTP HEAD/GET 都支持),让它一直醒着。
- **每张计算**:热机后,加水印本身约 **3-6 秒**(免费共享 CPU 上的隐形取证水印天然要几秒)。
- **画质 vs 速度**:工作分辨率上限由 `WM_MAX_SIDE` 控制,**默认 1600**(社媒/网页足够清晰、又安全)。
  调小(如 `1280`)更快;调大(如 `2000+`)更清晰但更慢、且在 512MB 免费机上有内存失败风险。
  想"又快又清晰又稳" → 升级 Render 付费实例(更快 CPU + 更多内存 + 不休眠)。

## 配置(环境变量)

| 变量 | 作用 |
|---|---|
| `WATERMARK_PASSWORD` | 登录口令(线上必设) |
| `WM_PASSWORD_IMG` / `WM_PASSWORD_WM` | 隐形水印的密钥种子(**只放服务器,别进 git**;改成只有你们知道的值) |
| `WM_MAX_SIDE` | 工作分辨率上限,默认 `1600` |
| `DATA_DIR` | 登记表/上传/输出目录(线上挂持久盘) |

## 功能清单(已做 / 没做)

### ✅ 已做

| 功能 |
|---|
| 可见 Logo(位置/浓淡/大小可调)+ 隐形 DWT-DCT 取证水印 |
| 验证页:拖图解码 → 显示编码 + 哪场婚礼(模糊匹配,抗压缩) |
| SQLite 登记表(编码 ↔ Shoot/Client、Note) |
| 真 DADA Logo,界面对齐官网极简黑底风格 |
| 三语界面(中文 / English / 印尼语),右上角一键切换 |
| **批量上传**(拖一批 + 打包 zip 下载) |
| 各种格式都吃(**HEIC** / PNG 透明 / CMYK / 16 位),自动转向 |
| 单张失败不影响整批,并提示真实原因 |
| 部署 Render + `/healthz` 保活端点 |

### ⬜ 没做 / 待定

| 状态 | 事项 |
|---|---|
| 待定 | 部署到子域名(`watermark.dada-island.com`)+ 作为 Notion **embed** 嵌入 |
| 可选 | 升级付费实例彻底解决冷启动 + 提速 + 支持更高分辨率 |
| 可选 | 接商业服务(Digimarc / Imatag)做全网盗图监测、抗裁剪 |

## 上线前记得两件事

1. `watermark.py` 里的 `PASSWORD_IMG`、`PASSWORD_WM` 是水印的钥匙。换成只有你们知道的值,别外传——谁拿到这两个数,谁就能读、甚至伪造水印。
2. 登记表 `registry.db` 和上传 / 输出目录都带着客户和作品信息,已经写进 `.gitignore`,不会进 git。
